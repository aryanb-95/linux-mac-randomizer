# linux-mac-randomizer
Tired of manually changing your MAC address? This simple Kali Linux Bash script automates the whole process using macchanger. It checks for root privileges, handles the network down/up cycles safely, and rotates your MAC at set intervals. Perfect for privacy and network security testing. Just drop it in, grab sudo, and let it run!
